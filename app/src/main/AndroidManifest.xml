<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cc.iceq.rss">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RssApplication"
        android:allowBackup="true"
        android:icon="@mipmap/rss_land_lanucher_black"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rss_land_lanucher_black_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RSSLand">
        <activity
            android:name=".RssDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.RSSLand.NoActionBar"
            />
        <activity
            android:name=".RssListActivity"
            android:label="@string/title_activity_note"
            android:theme="@style/Theme.RSSLand.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ScrollingActivity"
            android:exported="false"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.RSSLand.NoActionBar" />
        <activity
            android:name=".AddRssActivity"
            android:label="@string/title_activity_note"
            android:theme="@style/Theme.RSSLand" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RSSLand.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>